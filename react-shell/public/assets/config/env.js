(function(window) {
    window.__env = window.__env || {};
    window.__env.contextPath = window.__env.contextPath || '/';

    window.__env.halvedPath = 'http://localhost:8080/halved-backend';
    window.__env.doubledPath = 'http://localhost:8080/doubled-backend';
}(this));